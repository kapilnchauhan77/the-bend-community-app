export function NativeFilterChip({ label, selected = false, removable = false, onRemove }: { label: string; selected?: boolean; removable?: boolean; onRemove?(): void }) { return <span className={`native-filter-chip native-control ${selected ? 'selected' : ''}`}>{label}{removable && <button type="button" aria-label={`Remove ${label} filter`} onClick={onRemove}>×</button>}</span> }
