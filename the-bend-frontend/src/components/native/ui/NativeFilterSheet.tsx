import { useEffect, useRef } from 'react'
/* eslint-disable react-hooks/exhaustive-deps -- trigger focus is intentionally captured for dialog cleanup. */
export function NativeFilterSheet({ open, title = 'Filters', onClose, returnFocusRef, children }: { open: boolean; title?: string; onClose(): void; returnFocusRef?: React.RefObject<HTMLElement | null>; children: React.ReactNode }) { const closeRef = useRef<HTMLButtonElement>(null); const sheetRef = useRef<HTMLDivElement>(null); useEffect(() => { if (!open) return; closeRef.current?.focus(); const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose(); if (e.key === 'Tab' && sheetRef.current) { const els = [...sheetRef.current.querySelectorAll<HTMLElement>('button,input,select,textarea,[tabindex]:not([tabindex="-1"])')].filter((el) => !el.hasAttribute('disabled')); if (!els.length) return; const i = els.indexOf(document.activeElement as HTMLElement); if (e.shiftKey && (i <= 0)) { e.preventDefault(); els.at(-1)?.focus() } else if (!e.shiftKey && i === els.length - 1) { e.preventDefault(); els[0]?.focus() } } }; document.addEventListener('keydown', onKey); return () => { document.removeEventListener('keydown', onKey); returnFocusRef?.current?.focus() } }, [open, onClose, returnFocusRef]); if (!open) return null; return <div className="native-sheet-backdrop" role="presentation" onMouseDown={(e) => { if (e.target === e.currentTarget) onClose() }}><div ref={sheetRef} role="dialog" aria-modal="true" aria-labelledby="native-sheet-title"><button ref={closeRef} type="button" aria-label="Close filters" onClick={onClose}>×</button><h2 id="native-sheet-title">{title}</h2>{children}</div></div> }
